export const API_ROUTES = {
  AUTH: {
    REGISTER: "/register",
    LOGIN: "/login",
    REFRESH: "/refresh",
    LOGOUT: "/logout",
    RESET_PASSWORD: "/reset-password",
    UPDATE_PASSWORD:"/update-password"
  },
} as const;